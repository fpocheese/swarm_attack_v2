# 场景设置与拦截策略总结（当前环境）

## 1. 当前场景与基础仿真参数

- 当前训练/评估主场景：`scenario_1`（4v4）
- 备选场景：
  - `scenario_2`：4v6（防守优势）
  - `scenario_3`：6v4（进攻优势）

### 全局参数（来自 `envs/fov_penetration/config.py`）

- 地图尺寸：`map_size = 2000 m`
- 高度范围：`z ∈ [0, 1000] m`
- 仿真步长：`dt = 0.01 s`
- 每回合最大步数：`max_steps = 6000`（总时长约 60s）
- HVT 位置：`(1200, 0, 0)`
- 视场半角：`30°`
- 探测范围：`2500 m`
- 碰撞双杀阈值：`collision_kill_range = 5 m`
- 命中 HVT 阈值：`hit_hvt_range = 5 m`

---

## 2. 双方初始部署（位置/速度/航向）

## 2.1 进攻方（Offensive, 4架）

### 初始位置中心与分布

- 中心：`(-1200, 0, 300)`
- 环形展开：`spread_xy = 150 m`（按等角度分布）
- 噪声：
  - 平面位置噪声：`±100 m`
  - 高度噪声：`±20 m`

### 初始速度与姿态

- 初始速度：`v_nominal = 60 m/s`
- 初始航向：
  - 基准航向指向 HVT（从进攻中心指向 HVT）
  - 航向噪声：`±0.2 rad`
- 初始航迹角（gamma）噪声：`±0.02 rad`

---

## 2.2 防守方（Defensive / Interceptor, 4架）

### 初始位置中心与分布

- 中心：`(600, 0, 350)`
- 环形展开：`spread_xy = 200 m`
- 噪声：
  - 平面位置噪声：`±100 m`
  - 高度噪声：`±30 m`

### 初始速度与姿态

- 初始速度：`v_nominal = 65 m/s`
- 初始航向：
  - 基准航向指向“进攻方初始质心”
  - 航向噪声：`±0.3 rad`
- 初始航迹角（gamma）噪声：`±0.1 rad`

---

## 3. 飞行器能力约束（速度/机动/变化率）

## 3.1 进攻方能力

### 速度能力

- `v_min = 40 m/s`
- `v_nominal = 60 m/s`
- `v_max = 80 m/s`

### 过载能力（机动能力）

- 纵向（nx）：`[-0.5, 2.0] g`
- 横向（ny）：`[-5.0, 5.0] g`
- 法向（nz）：`[-0.5, 1.5] g`

### 过载变化率上限

- `dnx_max = 6.0 g/s`
- `dny_max = 12.0 g/s`
- `dnz_max = 3.0 g/s`

### 航迹角范围

- `gamma ∈ [-15°, 15°]`

### 动作缩放

- `action_scale = 1.0`（全幅机动）

---

## 3.2 防守方（拦截器）能力

### 速度能力

- `v_min = 50 m/s`
- `v_nominal = 65 m/s`
- `v_max = 85 m/s`

### 过载能力（机动能力）

- 纵向（nx）：`[-1.0, 3.0] g`
- 横向（ny）：`[-8.0, 8.0] g`
- 法向（nz）：`[-3.0, 3.0] g`

### 过载变化率上限

- `dnx_max = 8.0 g/s`
- `dny_max = 15.0 g/s`
- `dnz_max = 8.0 g/s`

### 航迹角范围

- `gamma ∈ [-45°, 45°]`

---

## 4. 动力学与控制映射

- 动力学模型：3D 固定翼质点模型
- 积分方式：`RK4`
- 控制输入：归一化动作 `a ∈ [-1,1]^3` 映射为 `(nx, ny, nz)`
- 关键约束：
  - 过载限幅
  - 过载变化率限幅（防止过快抖动）
  - 速度上下界
  - 航迹角上下界

---

## 5. 拦截方策略总结（当前实现）

## 5.1 初始目标分配（仅回合开始一次）

来自 `target_assignment.py`：

- 使用代价矩阵做初始目指分配（默认 `hungarian`）
- 代价综合两部分：
  - 目标威胁（越威胁越应优先）
  - 拦截代价（距离、闭合速度）
- 该分配仅用于初始飞行参考，不做周期性重分配

## 5.2 锁定状态机

来自 `policies_interceptor.py`：

- 状态：`INIT_GUIDE → LOCKED → MISSED / ABANDONED`
- 核心机制：
  1. `INIT_GUIDE`：按初始目指飞行并扫描视场
  2. 首个进入视场的进攻机触发锁定（FOV trigger lock）
  3. `LOCKED`：进入 3D PN 制导追击
  4. 目标长时间脱离视场（超过宽限）转 `MISSED`

## 5.3 当前目标切换规则（已按你的要求）

- **不再按“最近距离”每步切换目标**
- 规则改为：
  - 一旦选定当前追击目标，就保持追击
  - 仅当当前目标失效（死亡或已命中HVT）时，才重选最近存活目标

## 5.4 3D PN 制导参数（防守方）

- 导引增益：`pn_nav_gain = 4`
- 视场内信息更新频率：`pn_direct_freq = 20 Hz`
- 低频导引更新频率：`pn_guide_freq = 5 Hz`
- 目标外推预测时域：`pn_extrapolate_horizon = 0.2 s`

说明：
- 制导使用 LOS 角速率 + 闭合速度计算需求过载
- 在低频信息更新条件下，使用 0.2s 外推补偿时延

---

## 6. 回合终止相关（便于理解策略行为）

- `success`：有进攻机命中 HVT
- `all_killed`：进攻机全部失效（死亡或已命中）
- `timeout`：达到 `max_steps`

---

## 7. 与你当前训练设置的对应关系

- 你当前训练强调长时域突防，因此 `max_steps = 6000` 非常关键
- 在该设置下，进攻方有足够时间接近 HVT，拦截器也有完整追击窗口
- 拦截策略现已采用“目标粘性锁定（sticky target）”，避免频繁切换导致的追击不稳定
